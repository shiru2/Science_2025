# 倍音体験ツール 要件定義書

## プロジェクト概要

- **プロジェクト名**: 倍音体験ツール（仮）
- **目的**: 科学イベントのブースで、小学5年生に「声は波であり、分解・操作できる」ことを体験してもらう
- **使用場面**: 科学イベントのブース展示

## コンセプト

> 声は目に見えないけど、波でできている。
> コンピュータで分解すると、たくさんの「音の層」が重なっているとわかる。
> 基音に倍音が乗っかって、あなただけの声になっている。
> 層を取り除くと声が変わる——これが「音を操作する」ということ。

### キーメッセージ

- **物理現象としての声** → 可視化できる
- **数学（フーリエ変換）** → 分解できる
- **プログラム** → 操作できる

## ターゲット

| 項目 | 内容 |
|------|------|
| 対象年齢 | 小学5年生程度 |
| 同伴者 | 保護者 |
| 進行 | 主催者（開発者）がレクチャーしながら進める |
| 人数 | PC1台につき1人 |

## 体験フロー（約10分）

| ステップ | 内容 | 時間目安 |
|----------|------|----------|
| 1. 導入 | 「声を録音してみよう」→ 数秒録音 | 1分 |
| 2. 可視化 | スペクトログラム表示「これがキミの声の正体だよ」 | 2分 |
| 3. 操作 | スライダーで周波数カット → 再生して変化を体験 | 5分 |
| 4. まとめ | 「声はたくさんの音の層でできている」を確認 | 2分 |

## 機能要件

### 必須（MVP）

- [ ] マイクから録音（数秒）
- [ ] スペクトログラム表示
- [ ] スライダー式カットオフ
  - 「ここより下を消す」（ローカット）
  - 「ここより上を消す」（ハイカット）
- [ ] 編集後の音声をリアルタイムで再生
- [ ] デモ用に事前録音した音声を読み込み可能
- [ ] モダンなUI

### あれば良い（将来）

- [ ] スペクトログラム画像の保存
- [ ] QRコードで画像を共有（持ち帰り用）

## 技術要件

### アーキテクチャ

Webアプリ構成（フロントエンド + Pythonバックエンド）

```
┌─────────────────────────────────────────┐
│            ブラウザ (UI)                 │
│         React + Vite                    │
│  - 録音操作                              │
│  - スペクトログラム表示                   │
│  - スライダー操作                        │
└─────────────┬───────────────────────────┘
              │ REST API / WebSocket
┌─────────────▼───────────────────────────┐
│        ローカルサーバー                   │
│        Python + FastAPI                 │
│  - 音声データ受信                        │
│  - 周波数分解 (FFT)                      │
│  - フィルタリング処理                    │
│  - 加工済み音声を返却                    │
└─────────────────────────────────────────┘
```

### 動作環境

| 項目 | 内容 |
|------|------|
| OS | Linux / macOS / Windows（クロスプラットフォーム） |
| ネットワーク | オフライン動作（ローカルサーバー） |
| ブラウザ | Chrome / Firefox / Edge（モダンブラウザ） |
| マイク | 外付けマイク対応 |

### 技術スタック

**フロントエンド**

| 項目 | 技術 |
|------|------|
| フレームワーク | React |
| ビルドツール | Vite |
| 言語 | TypeScript |
| スタイリング | Tailwind CSS（予定） |

**バックエンド**

| 項目 | 技術 |
|------|------|
| フレームワーク | FastAPI |
| 言語 | Python |
| パッケージ管理 | uv |

**音声処理ライブラリ**

- NumPy: 数値計算
- SciPy: 周波数変換（FFT）、WAVファイル処理
- sounddevice: 音声入力・再生（バックエンド側で使用する場合）

### 環境構築手順

各PCで以下の手順を実行する。

**1. 前提条件**

- Python 3.11以上
- Node.js 18以上
- uv（Pythonパッケージ管理）
- npm（Node.jsパッケージ管理）

**2. リポジトリのクローン**

```bash
git clone <repository-url>
cd science-child
```

**3. バックエンドのセットアップ**

```bash
cd backend
uv sync
```

**4. フロントエンドのセットアップ**

```bash
cd frontend
npm install
npm run build
```

**5. 起動**

```bash
# バックエンドを起動
cd backend
uv run python server.py

# ブラウザで開く
# http://localhost:8000
```

### ディレクトリ構成（想定）

```
science-child/
├── README.md
├── requirements.md        # この要件定義書
├── backend/
│   ├── pyproject.toml
│   ├── server.py          # FastAPIサーバー
│   └── audio_processor.py # 音声処理ロジック
├── frontend/
│   ├── package.json
│   ├── src/
│   │   ├── App.tsx
│   │   ├── components/
│   │   │   ├── Recorder.tsx      # 録音UI
│   │   │   ├── Spectrogram.tsx   # スペクトログラム表示
│   │   │   └── FrequencySlider.tsx # カットオフスライダー
│   │   └── ...
│   └── ...
└── demo/
    └── sample_voice.wav   # デモ用音声
```

## 非機能要件

- **操作性**: シンプルで子供が「自分でやった」と感じられる
- **レスポンス**: スライダー操作→再生がスムーズ（処理が軽い）
- **デザイン**: モダンな見た目

## 補足

### 既存ツールとの関係

このプロジェクトは既存の `spectral_remover.py` の音声処理ロジックを参考に、Webアプリとして新規開発する。

主な変更点：
1. Matplotlib GUI → React Webアプリ
2. マウス範囲選択 → スライダー式カットオフ
3. 単体Python → フロントエンド/バックエンド分離構成
4. 複数PCで同じ環境を再現可能に

---

*作成日: 2025年5月*
*更新日: 2025年5月*
*プロジェクト: science-child*
